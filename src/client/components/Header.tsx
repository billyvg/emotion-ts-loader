import React from 'react';
import styled from '@emotion/styled';
import { css } from '@emotion/core';

export const Header = () => (
  <div
    css={css`
      color: red;
    `}
  >
    Test
  </div>
);
